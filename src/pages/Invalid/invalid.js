import React from 'react'
import Invalid from '../../components/Invalid-Account'
import { Col, Row, Container } from '../../components/Grid'

function invalidPage(){
    return(
    <div>
            <Invalid/>
    </div>
    )
}

export default invalidPage